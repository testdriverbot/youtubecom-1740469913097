version: 4.2.12
session: 67bd78bd8dfb545182f1811c
steps:
  - prompt: Focus the browser window.
    commands:
      - command: focus-application
        name: Google Chrome
  - prompt: Assert that the YouTube logo is present.
    commands:
      - command: assert
        expect: the YouTube logo is present
  - prompt: Assert that the "Search" bar is visible.
    commands:
      - command: assert
        expect: the "Search" bar is visible
  - prompt: >-
      Assert that the "Sign In" button is visible.  # Use the content of the
      file from the environment
    commands:
      - command: assert
        expect: the "Sign In" button is visible
